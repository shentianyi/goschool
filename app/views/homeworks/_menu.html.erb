LEFT MENU:
<ul>
     <%@menus.each do |menu|%>
     <li>
          <a href="#" type="<%= menu.value%>" class="homework-menu-a"><%= menu.display%></a>
          <div class="homework-list">
               <%if @menu_type==menu.value%>
               <%= render 'menu_item'%>
               <%end%>
          </div>
     </li>
     <% end %>
</ul>

