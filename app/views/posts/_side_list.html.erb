<div class="left">
  <% if @posts %>
      <% @menus.each do |menu| %>
          <div class="ui accordion" id="accordion">
            <div class="title post-menu-a <%= 'pin' if menu.is_pin %>" pin="<%= menu.is_pin %>" type="<%= menu.value %>">
              <i class="dropdown icon"></i>
              <%= menu.display %>
            </div>
            <div class="content">
              <% if @menu_type==menu.value %>
                  <%= render 'list_item' %>
              <% end %>
            </div>
          </div>
      <% end %>
  <% end %>
</div>

